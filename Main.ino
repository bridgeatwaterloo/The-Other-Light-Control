/* This is the main file for the interactive light project. It will be uploaded to each Arduino.
	The code that animates the lights is saved in separate dile called lights.h
	The code that reads from the sensor is saved in a se[arate file ca]led sensors.h 
*/
int pin = 9; //variable value
int count= 0;//counter starts with 0 value 

void setup(){
  pinMode(pin, OUTPUT);
  }

void threeFlashes(){}


	     

 